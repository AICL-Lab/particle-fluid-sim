# Git Pages 国际化实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将现有自定义文档系统迁移到 Docusaurus 3.x，实现中英文双语支持。

**Architecture:** 创建独立的 docs-site 目录作为 Docusaurus 项目，保留原有 Vite 构建的演示应用。Docusaurus 负责文档站点和主页，演示应用通过 static 目录集成。

**Tech Stack:** Docusaurus 3.x, React 18, TypeScript, GitHub Pages

---

## 文件结构

```
docs-site/                         # 新建 Docusaurus 项目
├── docusaurus.config.ts          # 主配置（i18n 设置）
├── sidebars.ts                   # 侧边栏配置
├── tsconfig.json                 # TypeScript 配置
├── package.json                  # Docusaurus 依赖
├── i18n/                         # 国际化目录
│   └── zh-CN/
│       ├── code.json             # UI 文本翻译
│       └── docusaurus.plugin-content-docs/
│           └── current/          # 中文文档
├── src/
│   ├── pages/
│   │   ├── index.tsx             # 英文主页
│   │   └── index.module.css      # 主页样式
│   ├── components/
│   │   ├── HeroSection/
│   │   ├── FeatureCard/
│   │   └── DemoEmbed/
│   └── css/
│       └── custom.css            # 全局样式
├── static/
│   └── demo/                     # 演示应用（构建时复制）
└── docs/                         # 英文文档源文件
    ├── intro.md
    ├── api.md
    ├── architecture.md
    └── ...

# 需要修改的现有文件
.github/workflows/pages.yml       # 更新部署流程
package.json                      # 添加 docs-site 脚本
```

---

## Task 1: 初始化 Docusaurus 项目

**Files:**

- Create: `docs-site/` 目录及所有初始化文件

- [ ] **Step 1: 创建 Docusaurus 项目**

```bash
cd /home/shane/dev/particle-fluid-sim
npx create-docusaurus@latest docs-site classic --typescript
```

预期：生成 `docs-site/` 目录，包含完整的 Docusaurus 项目结构。

- [ ] **Step 2: 验证项目初始化成功**

```bash
ls -la docs-site/
```

预期：看到 `docusaurus.config.ts`, `package.json`, `src/`, `docs/` 等文件和目录。

- [ ] **Step 3: 提交初始化的项目**

```bash
git add docs-site/
git commit -m "chore: initialize Docusaurus project for i18n docs"
```

---

## Task 2: 配置国际化

**Files:**

- Modify: `docs-site/docusaurus.config.ts`

- [ ] **Step 1: 配置 i18n 设置**

修改 `docs-site/docusaurus.config.ts`，添加 i18n 配置：

```typescript
// docs-site/docusaurus.config.ts
import type { Config } from '@docusaurus/types';
import type * as Preset from '@docusaurus/preset-classic';

const config: Config = {
  title: 'WebGPU Particle Fluid Simulation',
  tagline: 'High-performance particle fluid simulation with GPU compute shaders',
  url: 'https://lessup.github.io',
  baseUrl: '/particle-fluid-sim/',
  onBrokenLinks: 'throw',
  onBrokenMarkdownLinks: 'warn',
  favicon: 'img/favicon.ico',

  // 国际化配置
  i18n: {
    defaultLocale: 'en',
    locales: ['en', 'zh-CN'],
    localeConfigs: {
      en: {
        label: 'English',
        direction: 'ltr',
      },
      'zh-CN': {
        label: '简体中文',
        direction: 'ltr',
      },
    },
  },

  presets: [
    [
      'classic',
      {
        docs: {
          sidebarPath: './sidebars.ts',
          editUrl: 'https://github.com/LessUp/particle-fluid-sim/tree/master/docs-site/',
        },
        theme: {
          customCss: './src/css/custom.css',
        },
      } satisfies Preset.Options,
    ],
  ],

  themeConfig: {
    navbar: {
      title: 'Particle Fluid',
      logo: {
        alt: 'Particle Fluid Logo',
        src: 'img/logo.svg',
      },
      items: [
        {
          type: 'doc',
          docId: 'intro',
          position: 'left',
          label: 'Docs',
        },
        {
          to: '/demo',
          label: 'Demo',
          position: 'left',
        },
        {
          href: 'https://github.com/LessUp/particle-fluid-sim',
          label: 'GitHub',
          position: 'right',
        },
        {
          type: 'localeDropdown',
          position: 'right',
        },
      ],
    },
    footer: {
      style: 'dark',
      copyright: `Copyright © ${new Date().getFullYear()} LessUp. Built with Docusaurus.`,
    },
  } satisfies Preset.ThemeConfig,
};

export default config;
```

- [ ] **Step 2: 创建中文 UI 翻译文件**

创建 `docs-site/i18n/zh-CN/code.json`：

```json
{
  "theme.ErrorPageContent.title": {
    "message": "页面未找到",
    "description": "The title of the 404 page"
  },
  "theme.NavBar.navAriaLabel": {
    "message": "主导航",
    "description": "The ARIA label for the main navigation"
  },
  "theme.SearchBar.label": {
    "message": "搜索",
    "description": "The ARIA label and placeholder for search input"
  },
  "theme.TOCCollapsible.toggleButtonLabel": {
    "message": "目录",
    "description": "The label used by the button on the collapsible TOC component"
  },
  "theme.CodeBlock.copied": {
    "message": "已复制",
    "description": "The copied button label on code blocks"
  },
  "theme.colorToggle.ariaLabel": {
    "message": "切换深色/浅色模式",
    "description": "The ARIA label for the color mode toggle"
  },
  "theme.colorToggle.ariaLabel.mode.dark": {
    "message": "深色模式",
    "description": "The name for the dark color mode"
  },
  "theme.colorToggle.ariaLabel.mode.light": {
    "message": "浅色模式",
    "description": "The name for the light color mode"
  },
  "theme.navbar.mobileLanguageDropdown.label": {
    "message": "选择语言",
    "description": "The label for the mobile language selector dropdown"
  },
  "theme.blog.paginator.navAriaLabel": {
    "message": "分页导航",
    "description": "The ARIA label for the blog pagination"
  },
  "theme.blog.post.paginator.navAriaLabel": {
    "message": "文章分页导航",
    "description": "The ARIA label for the blog post pagination"
  }
}
```

- [ ] **Step 3: 提交国际化配置**

```bash
git add docs-site/docusaurus.config.ts docs-site/i18n/
git commit -m "feat(docs): configure Docusaurus i18n for en and zh-CN"
```

---

## Task 3: 创建自定义主页组件

**Files:**

- Create: `docs-site/src/pages/index.tsx`
- Create: `docs-site/src/pages/index.module.css`
- Create: `docs-site/src/components/HeroSection/index.tsx`
- Create: `docs-site/src/components/FeatureCard/index.tsx`
- Create: `docs-site/src/components/DemoEmbed/index.tsx`

- [ ] **Step 1: 创建 HeroSection 组件**

创建 `docs-site/src/components/HeroSection/index.tsx`：

```tsx
import React from 'react';
import styles from './styles.module.css';

interface HeroSectionProps {
  title: string;
  subtitle: string;
  badge?: string;
  stats?: Array<{ value: string; label: string }>;
  actions?: React.ReactNode;
}

export default function HeroSection({
  title,
  subtitle,
  badge,
  stats,
  actions,
}: HeroSectionProps): JSX.Element {
  return (
    <section className={styles.hero}>
      <div className={styles.heroBg}>
        {[...Array(6)].map((_, i) => (
          <div
            key={i}
            className={styles.particle}
            style={
              {
                '--delay': i,
                '--x': `${20 + i * 12}%`,
                '--y': `${20 + (i % 3) * 20}%`,
              } as React.CSSProperties
            }
          />
        ))}
      </div>
      <div className={`container ${styles.heroContent}`}>
        {badge && <div className={styles.heroBadge}>{badge}</div>}
        <h1>{title}</h1>
        <p className={styles.heroSubtitle}>{subtitle}</p>
        {stats && stats.length > 0 && (
          <div className={styles.heroStats}>
            {stats.map((stat, i) => (
              <React.Fragment key={i}>
                <div className={styles.stat}>
                  <span className={styles.statValue}>{stat.value}</span>
                  <span className={styles.statLabel}>{stat.label}</span>
                </div>
                {i < stats.length - 1 && <div className={styles.statDivider} />}
              </React.Fragment>
            ))}
          </div>
        )}
        {actions && <div className={styles.heroActions}>{actions}</div>}
      </div>
    </section>
  );
}
```

- [ ] **Step 2: 创建 HeroSection 样式**

创建 `docs-site/src/components/HeroSection/styles.module.css`：

```css
.hero {
  position: relative;
  padding: 4rem 0 6rem;
  overflow: hidden;
  background: linear-gradient(
    135deg,
    var(--ifm-color-primary-dark) 0%,
    var(--ifm-color-primary) 100%
  );
}

[data-theme='dark'] .hero {
  background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
}

.heroBg {
  position: absolute;
  inset: 0;
  overflow: hidden;
  pointer-events: none;
}

.particle {
  position: absolute;
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.3);
  animation: float 3s ease-in-out infinite;
  animation-delay: calc(var(--delay) * 0.5s);
  left: var(--x);
  top: var(--y);
}

@keyframes float {
  0%,
  100% {
    transform: translateY(0) scale(1);
    opacity: 0.3;
  }
  50% {
    transform: translateY(-20px) scale(1.2);
    opacity: 0.6;
  }
}

.heroContent {
  position: relative;
  z-index: 1;
  text-align: center;
}

.heroBadge {
  display: inline-block;
  padding: 0.25rem 0.75rem;
  margin-bottom: 1rem;
  font-size: 0.875rem;
  font-weight: 600;
  color: var(--ifm-color-white);
  background: rgba(255, 255, 255, 0.15);
  border-radius: 9999px;
}

.hero h1 {
  font-size: 3rem;
  font-weight: 700;
  color: var(--ifm-color-white);
  margin-bottom: 1rem;
}

.heroSubtitle {
  font-size: 1.25rem;
  color: rgba(255, 255, 255, 0.9);
  max-width: 600px;
  margin: 0 auto 2rem;
}

.heroStats {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 1.5rem;
  margin-bottom: 2rem;
}

.stat {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.statValue {
  font-size: 2rem;
  font-weight: 700;
  color: var(--ifm-color-white);
}

.statLabel {
  font-size: 0.875rem;
  color: rgba(255, 255, 255, 0.7);
}

.statDivider {
  width: 1px;
  height: 40px;
  background: rgba(255, 255, 255, 0.3);
}

.heroActions {
  display: flex;
  justify-content: center;
  gap: 1rem;
  flex-wrap: wrap;
}
```

- [ ] **Step 3: 创建 FeatureCard 组件**

创建 `docs-site/src/components/FeatureCard/index.tsx`：

```tsx
import React from 'react';
import styles from './styles.module.css';

interface FeatureCardProps {
  title: string;
  description: string;
  icon?: React.ReactNode;
}

export default function FeatureCard({ title, description, icon }: FeatureCardProps): JSX.Element {
  return (
    <div className={styles.featureCard}>
      {icon && <div className={styles.featureIcon}>{icon}</div>}
      <h3>{title}</h3>
      <p>{description}</p>
    </div>
  );
}
```

- [ ] **Step 4: 创建 FeatureCard 样式**

创建 `docs-site/src/components/FeatureCard/styles.module.css`：

```css
.featureCard {
  padding: 1.5rem;
  background: var(--ifm-card-background-color);
  border-radius: var(--ifm-card-border-radius);
  box-shadow: var(--ifm-global-shadow-lw);
  transition:
    transform 0.2s ease,
    box-shadow 0.2s ease;
}

.featureCard:hover {
  transform: translateY(-4px);
  box-shadow: var(--ifm-global-shadow-md);
}

.featureIcon {
  width: 48px;
  height: 48px;
  margin-bottom: 1rem;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(
    135deg,
    var(--ifm-color-primary) 0%,
    var(--ifm-color-primary-dark) 100%
  );
  border-radius: 12px;
  color: white;
}

.featureCard h3 {
  margin-bottom: 0.5rem;
  font-size: 1.125rem;
}

.featureCard p {
  margin: 0;
  color: var(--ifm-color-content-secondary);
  font-size: 0.875rem;
}
```

- [ ] **Step 5: 创建英文主页**

创建 `docs-site/src/pages/index.tsx`：

```tsx
import React from 'react';
import Layout from '@theme/Layout';
import HeroSection from '@site/src/components/HeroSection';
import FeatureCard from '@site/src/components/FeatureCard';
import styles from './index.module.css';

const features = [
  {
    title: 'GPU Compute Shaders',
    description:
      'All physics calculations run in parallel on the GPU using WebGPU compute shaders for maximum performance.',
  },
  {
    title: 'Frame-Rate Independent',
    description:
      'Physics uses delta time calculations, ensuring consistent simulation speed at any frame rate.',
  },
  {
    title: 'Adaptive Quality',
    description:
      'Automatically detects device capabilities and scales particle count from 2,500 to 10,000 for optimal performance.',
  },
  {
    title: 'Visual Effects',
    description:
      'Beautiful motion trails with velocity-based color mapping from cyan to purple based on particle speed.',
  },
  {
    title: 'Interactive Controls',
    description:
      'Push particles with mouse or touch input. HiDPI-aware coordinate mapping for Retina displays.',
  },
  {
    title: 'Modern Stack',
    description:
      'Built with TypeScript, Vite, WebGPU, and Vitest for a great developer experience.',
  },
];

export default function Home(): JSX.Element {
  return (
    <Layout
      title="WebGPU Particle Fluid Simulation"
      description="High-performance WebGPU particle fluid simulation with 10,000+ particles, GPU compute shaders, and real-time physics."
    >
      <HeroSection
        title="Particle Fluid Simulation"
        subtitle="A spec-driven WebGPU reference project with 10,000+ particles, compute-shader physics, adaptive quality, and a polished browser demo you can inspect and extend."
        badge="WebGPU Powered"
        stats={[
          { value: '10K+', label: 'Particles' },
          { value: '60', label: 'FPS Target' },
          { value: 'GPU', label: 'Accelerated' },
        ]}
        actions={
          <>
            <a className="button button--primary button--lg" href="/demo">
              🚀 Launch Demo
            </a>
            <a className="button button--secondary button--lg" href="/docs/intro">
              📖 Explore Specs
            </a>
          </>
        }
      />
      <main>
        <section className={styles.section}>
          <div className="container">
            <h2 className={styles.sectionTitle}>Features</h2>
            <p className={styles.sectionSubtitle}>
              Built with modern web technologies for maximum performance
            </p>
            <div className={styles.featuresGrid}>
              {features.map((feature, idx) => (
                <FeatureCard key={idx} {...feature} />
              ))}
            </div>
          </div>
        </section>
      </main>
    </Layout>
  );
}
```

- [ ] **Step 6: 创建主页样式**

创建 `docs-site/src/pages/index.module.css`：

```css
.section {
  padding: 4rem 0;
}

.sectionTitle {
  text-align: center;
  font-size: 2rem;
  margin-bottom: 0.5rem;
}

.sectionSubtitle {
  text-align: center;
  color: var(--ifm-color-content-secondary);
  margin-bottom: 3rem;
}

.featuresGrid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1.5rem;
}

@media (max-width: 996px) {
  .featuresGrid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 768px) {
  .featuresGrid {
    grid-template-columns: 1fr;
  }
}
```

- [ ] **Step 7: 创建中文主页**

创建 `docs-site/i18n/zh-CN/docusaurus-plugin-content-pages/index.tsx`：

```tsx
import React from 'react';
import Layout from '@theme/Layout';
import HeroSection from '@site/src/components/HeroSection';
import FeatureCard from '@site/src/components/FeatureCard';
import styles from '@site/src/pages/index.module.css';

const features = [
  {
    title: 'GPU 计算着色器',
    description: '所有物理计算使用 WebGPU 计算着色器在 GPU 上并行运行，实现最高性能。',
  },
  {
    title: '帧率无关',
    description: '物理计算使用增量时间，确保在任何帧率下都能保持一致的模拟速度。',
  },
  {
    title: '自适应质量',
    description: '自动检测设备性能，动态调整粒子数量（2,500 到 10,000）以获得最佳性能。',
  },
  {
    title: '视觉效果',
    description: '基于速度的颜色映射，粒子速度从青色到紫色渐变，呈现美丽的运动轨迹。',
  },
  {
    title: '交互控制',
    description: '通过鼠标或触摸推动粒子。支持 HiDPI 坐标映射，适配 Retina 显示屏。',
  },
  {
    title: '现代技术栈',
    description: '使用 TypeScript、Vite、WebGPU 和 Vitest 构建，提供出色的开发体验。',
  },
];

export default function Home(): JSX.Element {
  return (
    <Layout
      title="WebGPU 粒子流体模拟"
      description="基于 WebGPU 的高性能粒子流体模拟，支持 10,000+ 粒子、GPU 计算着色器和实时物理计算。"
    >
      <HeroSection
        title="粒子流体模拟"
        subtitle="基于 WebGPU 的规范驱动参考项目，支持 10,000+ 粒子、计算着色器物理、自适应质量，以及可检查和扩展的精美浏览器演示。"
        badge="WebGPU 驱动"
        stats={[
          { value: '10K+', label: '粒子数' },
          { value: '60', label: '目标帧率' },
          { value: 'GPU', label: '加速' },
        ]}
        actions={
          <>
            <a className="button button--primary button--lg" href="/demo">
              🚀 启动演示
            </a>
            <a className="button button--secondary button--lg" href="/docs/intro">
              📖 查看规范
            </a>
          </>
        }
      />
      <main>
        <section className={styles.section}>
          <div className="container">
            <h2 className={styles.sectionTitle}>功能特性</h2>
            <p className={styles.sectionSubtitle}>使用现代 Web 技术构建，实现最高性能</p>
            <div className={styles.featuresGrid}>
              {features.map((feature, idx) => (
                <FeatureCard key={idx} {...feature} />
              ))}
            </div>
          </div>
        </section>
      </main>
    </Layout>
  );
}
```

- [ ] **Step 8: 提交主页组件**

```bash
git add docs-site/src/pages/ docs-site/src/components/ docs-site/i18n/zh-CN/docusaurus-plugin-content-pages/
git commit -m "feat(docs): create custom homepage components with i18n support"
```

---

## Task 4: 迁移 Markdown 文档

**Files:**

- Create: `docs-site/docs/` 目录下的英文文档
- Create: `docs-site/i18n/zh-CN/docusaurus.plugin-content-docs/current/` 目录下的中文文档

- [ ] **Step 1: 创建英文文档目录结构**

```bash
mkdir -p docs-site/docs
```

- [ ] **Step 2: 复制并调整 README 为 intro.md**

创建 `docs-site/docs/intro.md`：

````markdown
---
sidebar_position: 1
slug: /
---

# WebGPU Particle Fluid Simulation

A high-performance particle fluid simulation running entirely on the GPU using WebGPU compute shaders. Features 10,000+ particles with real-time physics, adaptive quality scaling, and a polished interactive demo.

## Quick Start

```bash
git clone https://github.com/LessUp/particle-fluid-sim.git
cd particle-fluid-sim
npm install
npm run dev
```
````

Open [http://localhost:5173](http://localhost:5173) in a WebGPU-compatible browser.

## Requirements

- Node.js 18+
- Chrome 113+, Edge 113+, or Safari 17+

## Documentation

- [API Reference](/docs/api) - TypeScript API documentation
- [Architecture](/docs/architecture) - System architecture overview
- [Performance Guide](/docs/performance) - Optimization tips
- [Troubleshooting](/docs/troubleshooting) - Common issues and solutions

````

- [ ] **Step 3: 创建中文 intro 文档**

创建 `docs-site/i18n/zh-CN/docusaurus.plugin-content-docs/current/intro.md`：

```markdown
---
sidebar_position: 1
slug: /
---

# WebGPU 粒子流体模拟

基于 WebGPU 计算着色器的高性能粒子流体模拟。支持 10,000+ 粒子的实时物理计算、自适应质量缩放，以及精美的交互式演示。

## 快速开始

```bash
git clone https://github.com/LessUp/particle-fluid-sim.git
cd particle-fluid-sim
npm install
npm run dev
````

在支持 WebGPU 的浏览器中打开 [http://localhost:5173](http://localhost:5173)。

## 系统要求

- Node.js 18+
- Chrome 113+、Edge 113+ 或 Safari 17+

## 文档

- [API 参考](/docs/api) - TypeScript API 文档
- [架构](/docs/architecture) - 系统架构概述
- [性能指南](/docs/performance) - 优化建议
- [故障排除](/docs/troubleshooting) - 常见问题及解决方案

````

- [ ] **Step 4: 创建其他英文文档占位符**

创建 `docs-site/docs/api.md`：

```markdown
---
sidebar_position: 2
---

# API Reference

> This is a placeholder. The full content will be migrated from `docs/API.md`.

See the full API documentation at [GitHub](https://github.com/LessUp/particle-fluid-sim/blob/master/docs/API.md).
````

创建 `docs-site/docs/architecture.md`：

```markdown
---
sidebar_position: 3
---

# Architecture

> This is a placeholder. The full content will be migrated from `docs/architecture/README.md`.

See the full architecture documentation at [GitHub](https://github.com/LessUp/particle-fluid-sim/blob/master/docs/architecture/README.md).
```

创建 `docs-site/docs/performance.md`：

```markdown
---
sidebar_position: 4
---

# Performance Guide

> This is a placeholder. The full content will be migrated from `docs/PERFORMANCE.md`.

See the full performance guide at [GitHub](https://github.com/LessUp/particle-fluid-sim/blob/master/docs/PERFORMANCE.md).
```

创建 `docs-site/docs/troubleshooting.md`：

```markdown
---
sidebar_position: 5
---

# Troubleshooting

> This is a placeholder. The full content will be migrated from `docs/TROUBLESHOOTING.md`.

See the full troubleshooting guide at [GitHub](https://github.com/LessUp/particle-fluid-sim/blob/master/docs/TROUBLESHOOTING.md).
```

- [ ] **Step 5: 创建其他中文文档占位符**

创建 `docs-site/i18n/zh-CN/docusaurus.plugin-content-docs/current/api.md`：

```markdown
---
sidebar_position: 2
---

# API 参考

> 此为占位符。完整内容将从 `docs/API.md` 迁移。

完整 API 文档请参见 [GitHub](https://github.com/LessUp/particle-fluid-sim/blob/master/docs/API.md)。
```

创建 `docs-site/i18n/zh-CN/docusaurus.plugin-content-docs/current/architecture.md`：

```markdown
---
sidebar_position: 3
---

# 架构

> 此为占位符。完整内容将从 `docs/architecture/README.md` 迁移。

完整架构文档请参见 [GitHub](https://github.com/LessUp/particle-fluid-sim/blob/master/docs/architecture/README.md)。
```

创建 `docs-site/i18n/zh-CN/docusaurus.plugin-content-docs/current/performance.md`：

```markdown
---
sidebar_position: 4
---

# 性能指南

> 此为占位符。完整内容将从 `docs/PERFORMANCE.md` 迁移。

完整性能指南请参见 [GitHub](https://github.com/LessUp/particle-fluid-sim/blob/master/docs/PERFORMANCE.md)。
```

创建 `docs-site/i18n/zh-CN/docusaurus.plugin-content-docs/current/troubleshooting.md`：

```markdown
---
sidebar_position: 5
---

# 故障排除

> 此为占位符。完整内容将从 `docs/TROUBLESHOOTING.md` 迁移。

完整故障排除指南请参见 [GitHub](https://github.com/LessUp/particle-fluid-sim/blob/master/docs/TROUBLESHOOTING.md)。
```

- [ ] **Step 6: 提交文档迁移**

```bash
git add docs-site/docs/ docs-site/i18n/zh-CN/docusaurus.plugin-content-docs/
git commit -m "feat(docs): migrate markdown docs with Chinese translations"
```

---

## Task 5: 配置侧边栏

**Files:**

- Modify: `docs-site/sidebars.ts`

- [ ] **Step 1: 配置侧边栏结构**

修改 `docs-site/sidebars.ts`：

```typescript
import type { SidebarsConfig } from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  docs: [
    'intro',
    {
      type: 'category',
      label: 'Reference',
      items: ['api', 'architecture', 'performance', 'troubleshooting'],
    },
  ],
};

export default sidebars;
```

- [ ] **Step 2: 提交侧边栏配置**

```bash
git add docs-site/sidebars.ts
git commit -m "feat(docs): configure sidebar navigation"
```

---

## Task 6: 更新自定义样式

**Files:**

- Modify: `docs-site/src/css/custom.css`

- [ ] **Step 1: 添加自定义样式**

修改 `docs-site/src/css/custom.css`，保留深色/浅色模式支持：

```css
/**
 * Any CSS included here will be global. The classic template
 * bundles Infima by default. Infima is a CSS framework designed to
 * work well with Docusaurus.
 */

:root {
  --ifm-color-primary: #00d4ff;
  --ifm-color-primary-dark: #00bfe6;
  --ifm-color-primary-darker: #00b3d9;
  --ifm-color-primary-darkest: #0094b3;
  --ifm-color-primary-light: #1ad8ff;
  --ifm-color-primary-lighter: #26daff;
  --ifm-color-primary-lightest: #4de2ff;
  --ifm-code-font-size: 95%;
  --ifm-font-family-base:
    -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Noto Sans SC', 'PingFang SC', 'Microsoft YaHei',
    Helvetica, Arial, sans-serif;
}

[data-theme='dark'] {
  --ifm-color-primary: #00d4ff;
  --ifm-color-primary-dark: #00bfe6;
  --ifm-color-primary-darker: #00b3d9;
  --ifm-color-primary-darkest: #0094b3;
  --ifm-color-primary-light: #1ad8ff;
  --ifm-color-primary-lighter: #26daff;
  --ifm-color-primary-lightest: #4de2ff;
  --ifm-background-color: #1a1a2e;
  --ifm-background-surface-color: #16213e;
}

/* Gradient text for hero */
.gradient-text {
  background: linear-gradient(135deg, #00d4ff 0%, #9966cc 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* Button styles */
.button--primary {
  background: linear-gradient(135deg, #00d4ff 0%, #9966cc 100%);
  border: none;
}

.button--primary:hover {
  background: linear-gradient(135deg, #00bfe6 0%, #8855bb 100%);
}

/* Card hover effects */
.card:hover {
  transform: translateY(-4px);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
}

/* Footer customization */
.footer {
  background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
}

/* Responsive adjustments */
@media (max-width: 996px) {
  :root {
    --ifm-font-size-base: 17px;
  }
}
```

- [ ] **Step 2: 提交自定义样式**

```bash
git add docs-site/src/css/custom.css
git commit -m "feat(docs): add custom styles with dark mode support"
```

---

## Task 7: 更新 GitHub Actions 部署流程

**Files:**

- Modify: `.github/workflows/pages.yml`

- [ ] **Step 1: 更新部署工作流**

修改 `.github/workflows/pages.yml`：

```yaml
name: Deploy Documentation to GitHub Pages

on:
  push:
    branches: [master]
    paths:
      - 'src/**'
      - 'index.html'
      - 'docs/**'
      - 'openspec/specs/**'
      - 'README.md'
      - 'README.zh-CN.md'
      - 'CHANGELOG.md'
      - 'CONTRIBUTING.md'
      - 'package.json'
      - 'package-lock.json'
      - 'vite.config.ts'
      - 'scripts/**'
      - 'docs-site/**'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install root dependencies
        run: npm ci

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Build demo
        run: VITE_BASE_PATH=/particle-fluid-sim/demo/ npm run build

      - name: Install Docusaurus dependencies
        working-directory: docs-site
        run: npm ci

      - name: Build Docusaurus site
        working-directory: docs-site
        run: npm run build

      - name: Copy demo to Docusaurus static
        run: |
          rm -rf docs-site/build/demo
          cp -r dist/ docs-site/build/demo/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs-site/build
          retention-days: 1

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
```

- [ ] **Step 2: 提交部署流程更新**

```bash
git add .github/workflows/pages.yml
git commit -m "feat(ci): update GitHub Actions for Docusaurus deployment"
```

---

## Task 8: 本地测试

**Files:**

- 无新文件

- [ ] **Step 1: 安装依赖并启动开发服务器**

```bash
cd docs-site
npm install
npm start
```

预期：浏览器自动打开 `http://localhost:3000`，显示英文主页。

- [ ] **Step 2: 测试中文版本**

访问 `http://localhost:3000/zh-CN/`。

预期：显示中文主页，导航栏有语言切换下拉菜单。

- [ ] **Step 3: 测试语言切换**

点击导航栏右侧的语言下拉菜单，切换语言。

预期：页面切换到对应语言版本，URL 更新。

- [ ] **Step 4: 测试构建**

```bash
npm run build
```

预期：构建成功，生成 `docs-site/build/` 目录，包含 `en/` 和 `zh-CN/` 子目录。

---

## Task 9: 最终提交和推送

**Files:**

- 无新文件

- [ ] **Step 1: 推送所有更改**

```bash
git push origin master
```

预期：触发 GitHub Actions 工作流，自动部署到 GitHub Pages。

- [ ] **Step 2: 验证部署**

等待 GitHub Actions 完成后，访问：

- 英文版：`https://lessup.github.io/particle-fluid-sim/`
- 中文版：`https://lessup.github.io/particle-fluid-sim/zh-CN/`

预期：两个页面正常显示，语言切换功能正常。

---

## 验证计划

1. **功能验证**
   - [ ] 语言切换按钮正常工作
   - [ ] 中英文页面内容正确
   - [ ] 深色/浅色模式切换
   - [ ] 导航链接正确

2. **SEO 验证**
   - [ ] hreflang 标签正确（查看页面源码）
   - [ ] 页面 lang 属性正确（`<html lang="en">` 或 `<html lang="zh-CN">`）
   - [ ] 站点地图包含所有语言版本（`/sitemap.xml`）

3. **性能验证**
   - [ ] 构建时间 < 2 分钟
   - [ ] 页面加载时间 < 3 秒
   - [ ] Lighthouse 分数 > 90
